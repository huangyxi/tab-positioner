// No script logic is required for this test extension.
// Note: The filename (e.g., `background.service_worker`) in the manifest must be unique
// from the main extension's service worker to prevent conflicts during testing.
